import React from "react";
import Bar, { Layout } from "../components/Bar";

const Home = () => {
  return (
    <Layout>
      <Bar />
      <div>home</div>
    </Layout>
  );
};

export default Home;
